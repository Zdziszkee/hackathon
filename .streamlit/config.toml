[theme]
base = "dark"
# Primary accent color used across widgets
primaryColor = "#e5e7eb"
# Main app background (deep charcoal)
backgroundColor = "#0a0a0a"
# Secondary surfaces (sidebar, cards, inputs)
secondaryBackgroundColor = "#141414"
# Default text color
textColor = "#fafafa"
font = "sans serif"